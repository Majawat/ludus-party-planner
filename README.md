# Ludus Party Planner

Self-hosted event registration for LAN parties and board game nights.

## Stack
Python, Flask, SQLite, DaisyUI, HTMX — runs in Docker.

## Setup
1. Copy `.env.example` to `.env` and fill in values
2. `docker compose up --build`
3. `flask create-admin` to promote your account to admin

## Development
See CLAUDE.md for full project spec and build sequence.
